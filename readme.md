# Ejercicio 1 - README

Este archivo describe cómo ejecutar el script `ej1.py`.

## Requisitos

Se requiere tener instalado Python 3 en el sistema. Para instalarlo, sigue los pasos según tu sistema operativo:

### En Ubuntu/Debian:
```bash
sudo apt update
sudo apt install python3
```

## Ejecución

Para ejecutar el archivo con la resolucion del ejercicio 2, utiliza el siguiente comando en la terminal:

```bash
cd DyC/
python3 ej1.py
```
# Ejercicio 2 

Este archivo describe cómo ejecutar el script `ej2.py`.

## Requisitos

Se requiere tener instalado Python 3 en el sistema. Para instalarlo, sigue los pasos según tu sistema operativo:

### En Ubuntu/Debian:
```bash
sudo apt update
sudo apt install python3
```

## Ejecución

Para ejecutar el archivo con la resolucion del ejercicio 2, utiliza el siguiente comando en la terminal:

```bash
cd greedy/
python3 ej2.py
```








# Ejercicio 3 

Este archivo describe cómo ejecutar el script `ej3.py`.

## Requisitos

Se requiere tener instalado Python 3 en el sistema. Para instalarlo, sigue los pasos según tu sistema operativo:

### En Ubuntu/Debian:
```bash
sudo apt update
sudo apt install python3
```

## Ejecución

Para ejecutar el archivo con la resolucion del ejercicio 2, utiliza el siguiente comando en la terminal:

```bash
cd backtracking/
python3 ej3.py
```
